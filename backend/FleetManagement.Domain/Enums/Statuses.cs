namespace FleetManagement.Domain.Enums;

public enum VehicleStatus { Available, OnTrip, UnderMaintenance }
public enum DriverStatus { Available, OnTrip, Inactive }
public enum OrderStatus { Draft, Assigned, InProgress, Completed, Cancelled }
public enum TripStatus { Draft, Assigned, InProgress, Completed, Cancelled }
